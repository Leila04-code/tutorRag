[project]
name = "server"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Leila04-code", email = "eddike2004@gmail.com" }
]
requires-python = ">=3.14"
dependencies = []

[project.scripts]
server = "server:main"

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
